import { execa } from "execa";

describe("create-module CLI", () => {
  // it("should execute the create-module script successfully", async () => {
  //   const { stdout } = await execa("node", ["./bin/create-module.sh"]);
  //   expect(stdout).toContain("Module created successfully");
  // });
});
